# ──────────────────────────────────────────────────────────────────────────────
# MAVI-Robot — Dependencias (Compatible con ARM64 / Termux / Linux)
#
# En Android/Termux:
#   1. Instalar librerías nativas primero:
#      pkg update && pkg install portaudio espeak -y
#   2. Instalar dependencias Python:
#      pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────────────────────

# Audio I/O
sounddevice>=0.4.6
soundfile>=0.12.0

# Procesamiento numérico y señales
numpy>=1.26.0
scipy>=1.10.0

# Speech-to-Text
SpeechRecognition>=3.10.0

# Compatibilidad Python 3.13+
audioop-lts>=0.2.0
standard-aifc>=3.13.0
standard-chunk>=3.13.0

# Utilidades
typing_extensions>=4.8.0
tqdm>=4.65.0
